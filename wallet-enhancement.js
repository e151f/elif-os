(()=>{try{
const KEY='elif-os-trading-v1',root=()=>document.querySelector('#view'),today=()=>{const d=new Date();return d.getFullYear()+'-'+String(d.getMonth()+1).padStart(2,'0')+'-'+String(d.getDate()).padStart(2,'0')},esc=s=>String(s??'').replace(/[&<>\"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;'}[c])),read=()=>{try{const s=JSON.parse(localStorage.getItem(KEY)||'{}');return {trades:Array.isArray(s.trades)?s.trades:[]}}catch{return {trades:[]}}},write=s=>localStorage.setItem(KEY,JSON.stringify(s)),money=n=>'₺'+Number(n||0).toLocaleString('tr-TR',{minimumFractionDigits:2,maximumFractionDigits:2});
function render(){const v=root();if(!v||v.dataset.page!=='wallet')return;const s=read(),tr=s.trades,d=today(),day=tr.filter(x=>x.date===d).reduce((n,x)=>n+Number(x.pnl||0),0),net=tr.reduce((n,x)=>n+Number(x.pnl||0),0),wins=tr.filter(x=>Number(x.pnl)>0).length,losses=tr.filter(x=>Number(x.pnl)<0).length;v.innerHTML=`<section><div class="viewhead"><div><h1>Wallet</h1><p>Trading journal, daily P/L and a clean record of what you learned.</p></div></div><div class="enh-grid three"><div class="panel enh-card"><small>TODAY'S P/L</small><div class="metric-big ${day>=0?'profit':'loss'}">${day>=0?'+':''}${money(day)}</div></div><div class="panel enh-card"><small>NET LOGGED P/L</small><div class="metric-big ${net>=0?'profit':'loss'}">${net>=0?'+':''}${money(net)}</div></div><div class="panel enh-card"><small>WIN RATE</small><div class="metric-big">${tr.length?Math.round(wins/tr.length*100):0}%</div><small>${wins} wins · ${losses} losses · ${tr.length} trades</small></div></div><div class="enh-grid" style="margin-top:14px"><div class="panel enh-card"><span class="tag">TRADE JOURNAL</span><h3>Log a trade</h3><form data-trading-form autocomplete="off"><div class="enh-grid"><div class="enh-field"><label>Date</label><input class="enh-input" name="date" type="date" value="${d}" required></div><div class="enh-field"><label>Asset</label><input class="enh-input" name="asset" placeholder="XAUUSD / BTC / NVDA" required></div></div><div class="enh-grid"><div class="enh-field"><label>Entry</label><input class="enh-input" name="entry" type="number" step="any" placeholder="Entry price"></div><div class="enh-field"><label>Exit</label><input class="enh-input" name="exit" type="number" step="any" placeholder="Exit price"></div></div><div class="enh-grid"><div class="enh-field"><label>Position / size</label><input class="enh-input" name="size" placeholder="e.g. 0.5 oz / 10 shares"></div><div class="enh-field"><label>P/L ₺</label><input class="enh-input" name="pnl" type="number" step="0.01" placeholder="+250 or -120" required></div></div><div class="enh-field"><label>Setup / strategy</label><input class="enh-input" name="strategy" placeholder="Breakout, support, trend, news..."></div><div class="enh-field"><label>Lesson / notes</label><textarea class="enh-textarea" style="min-height:140px" name="notes" placeholder="Why did I enter? What was my mistake or what worked? What will I do differently next time?"></textarea></div><button class="primary" type="submit">save trade</button></form></div><div class="panel enh-card"><span class="tag">RECENT TRADES</span><h3>Trading history</h3>${tr.slice(-12).reverse().map(x=>`<div class="check"><div class="grow"><b>${esc(x.asset)}</b><br><small>${esc(x.date)} · ${esc(x.strategy||'No strategy')}</small></div><b class="${Number(x.pnl)>=0?'profit':'loss'}">${Number(x.pnl)>=0?'+':''}${money(x.pnl)}</b></div>`).join('')||'<div class="empty-state">No trades logged yet.</div>'}</div></div><div class="panel enh-card" style="margin-top:14px"><span class="tag">TRADING DISCIPLINE</span><div class="enh-grid three"><div><b>1. Plan</b><br><small>Define the setup before entering.</small></div><div><b>2. Execute</b><br><small>Record the actual entry, exit and P/L.</small></div><div><b>3. Review</b><br><small>Write the lesson, not just the result.</small></div></div></div></section>`}
document.addEventListener('submit',e=>{const f=e.target.closest('[data-trading-form]');if(!f)return;e.preventDefault();e.stopImmediatePropagation();const fd=new FormData(f),s=read(),p=Number(fd.get('pnl')||0);s.trades.push({id:Date.now(),date:fd.get('date')||today(),asset:String(fd.get('asset')||'').trim(),entry:fd.get('entry'),exit:fd.get('exit'),size:fd.get('size'),pnl:p,strategy:String(fd.get('strategy')||'').trim(),notes:String(fd.get('notes')||'').trim()});write(s);render();const t=document.querySelector('#toast');if(t){t.textContent='Trade saved ✓';t.classList.add('show');setTimeout(()=>t.classList.remove('show'),1400)}});
window.addEventListener('click',e=>{const b=e.target.closest('button[data-page="wallet"]');if(b)setTimeout(render,0)});window.ELIFWalletEnhancement={render};
}catch(e){console.warn('ELIF Wallet enhancement disabled',e)}})();